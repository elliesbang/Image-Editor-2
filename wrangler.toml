name = "image-editor"
compatibility_date = "2025-10-13"

[build]
command = "npm install --prefer-online --legacy-peer-deps --force && npm run build"
cwd = "."
pages_build_output_dir = "dist"

[env.production]
NODE_ENV = "production"
